import 'package:flutter/material.dart';
import '../widgets/list_favorite_repos.dart';

class FavoriteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListFavoriteRepos();
  }
}
